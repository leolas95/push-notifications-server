{
  "name": "push-notifications-server",
  "version": "1.0.0",
  "description": "Very simple server to offer an endpoint to send push notifications to a device or group of devices",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Leonardo Guedez",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4",
    "firebase-admin": "^7.1.1"
  }
}
